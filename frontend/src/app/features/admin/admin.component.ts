import { ChangeDetectionStrategy, Component, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
@Component({selector:'app-admin',imports:[FormsModule],changeDetection:ChangeDetectionStrategy.OnPush,template:`<section><header><span class="mark">N</span><div><h1>مدیریت نورستان</h1><p>ورود امن برای مدیران وب‌سایت</p></div></header><form (submit)="$event.preventDefault();submitted.set(true)"><label>پست الکترونیک<input type="email" autocomplete="username" required [(ngModel)]="email" name="email"></label><label>رمز عبور<input type="password" autocomplete="current-password" required [(ngModel)]="password" name="password"></label><button type="submit">ورود به مدیریت</button>@if(submitted()){<p class="message">اتصال ورود پس از پیکربندی مالک اولیه فعال می‌شود.</p>}</form><a href="/">بازگشت به وب‌سایت</a></section>`,styles:[`:host{display:grid;place-items:center;min-height:75vh}section{width:min(100% - 2rem,28rem);padding:2rem;border:1px solid var(--color-border);border-radius:var(--radius-lg);background:var(--color-surface);box-shadow:var(--shadow-surface)}header{display:flex;align-items:center;gap:1rem;margin-block-end:2rem}.mark{display:grid;place-items:center;width:3rem;height:3rem;border:1px solid var(--color-primary);border-radius:.8rem;color:var(--color-primary);box-shadow:var(--shadow-glow)}h1{margin:0;font-size:1.4rem}header p{margin:0;color:var(--color-text-muted);font-size:.75rem}label{display:block;margin-block:1rem;color:var(--color-text-muted);font-size:.8rem}input{display:block;width:100%;margin-block-start:.4rem;padding:.8rem;border:1px solid var(--color-border);border-radius:.6rem;background:var(--color-bg);color:var(--color-text)}button{width:100%;padding:.85rem;border:0;border-radius:999px;background:var(--color-primary);color:var(--color-primary-contrast);font-weight:800}section>a{display:block;margin-block-start:1.5rem;color:var(--color-text-muted);font-size:.75rem;text-align:center}.message{color:var(--color-primary);font-size:.75rem;text-align:center}`]}) export class AdminComponent{protected email='';protected password='';protected readonly submitted=signal(false)}
